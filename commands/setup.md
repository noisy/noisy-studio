---
description: Set up Noisy Studio voice with the native app
---

Use the setup skill in `skills/setup/SKILL.md`. Confirm the native app is
installed and running, select the intended endpoint explicitly, then verify
speech in both directions in this conversation. Keep credentials in the app.

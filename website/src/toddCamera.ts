// Website assets already include the approved crop. Do not apply a second zoom.
// Keep replacements lightweight: rebuild from original camera files using
// tools/website-media/prepare_todd.py and todd-framing.json (see README.md).
export const toddCamera = {
  cameraZoom: 1,
  cameraOffsetX: 0,
  cameraOffsetY: 0,
};
